version: 1.0
runtime: python3 
build:
  commands:
    build:
      - python -m venv env
      - env\Scripts\activate
      - pip install -r requirements.txt 
run: 
  command: 
      - manage.py migrate
      - manage.py createsuperuser 
      - manage.py runserver